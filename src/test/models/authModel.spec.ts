import { expect } from "chai";
import "mocha";
// import { TokenObject, Token } from "../../models/authModel";

describe("AuthModel", () => {
	// it("should return an encoded token", () => {
	// 	const token = new Token().generateAccessToken("UserA", 100);
	// 	expect(token).to.have.length(261);
	// });

	// it("should return an instance of TokenObject", () => {
	// 	const token = new Token().generateAccessToken("UserA", 200);
	// 	const decoded = new Token().decode(token);
	// 	expect(decoded).to.be.an.instanceof(TokenObject);
	// 	expect(decoded).to.have.property("iss");
	// 	expect(decoded).to.have.property("jti");
	// 	expect(decoded).to.have.property("iat");
	// 	expect(decoded).to.have.property("exp");
	// 	expect(decoded).to.have.property("userId");
	// 	expect(decoded).to.have.property("isRefreshToken");
	// 	expect(decoded).to.have.property("roleId");
	// });
});